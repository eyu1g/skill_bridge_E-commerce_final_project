# MongoDB connection string (MongoDB Atlas recommended for transactions)
MONGODB_URI="mongodb://localhost:27017/skillbridge"

# JWT secret key
JWT_SECRET="change_this_to_a_strong_secret"

# App configuration
PORT=3000
NODE_ENV=development
